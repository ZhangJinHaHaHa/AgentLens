/**
 * PopoWelcome — the actual popup logic lives in index.html <script>
 * to avoid React StrictMode double-invocation issues.
 * This component is a no-op placeholder kept for import compatibility.
 */
export function PopoWelcome(): null {
  return null;
}
